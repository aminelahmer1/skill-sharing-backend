spring:
  application:
    name: service-user
  datasource:
    url: jdbc:postgresql://localhost:5432/User_SkillSharing
    username: postgres
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  security:
    user:
      name: admin
      password: admin

server:
  port: 8081